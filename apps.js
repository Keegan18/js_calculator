//Number variables//
var output;

function calculator(x, y, z){
if (x == "add", "sum",) {
	output = y + z;
		}
		if else (x == "subtract", "difference",){
			output = y - z;
		}
		if else (x == "multiply", "product"){
			output = y * z;
		}
		if else (x == "divide", "quotient"){
			output = y / z;
		}
else {
	output = "Error, remove McAfee and install Norton"
}
}
calculator ("multiply", 34, 59);
console.log (output);